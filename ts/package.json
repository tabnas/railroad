{
  "name": "@tabnas/railroad",
  "version": "0.3.0",
  "main": "dist/railroad.js",
  "type": "commonjs",
  "types": "dist/railroad.d.ts",
  "description": "Railroad (syntax) diagram renderer for the tabnas parser.",
  "homepage": "https://github.com/rjrodger/tabnas-railroad",
  "keywords": [
    "tabnas",
    "railroad",
    "syntax",
    "diagram",
    "grammar",
    "parser",
    "plugin"
  ],
  "author": "Richard Rodger (http://richardrodger.com)",
  "repository": {
    "type": "git",
    "url": "git://github.com/tabnas/railroad.git",
    "directory": "ts"
  },
  "scripts": {
    "test": "node --enable-source-maps --test test/**/*.test.js",
    "watch": "tsc --build src -w",
    "build": "tsc --build src",
    "clean": "rm -rf node_modules dist dist-test yarn.lock package-lock.json",
    "reset": "npm run clean && npm i && npm run build && npm test",
    "repo-tag": "REPO_VERSION=`node -e \"console.log(require('./package').version)\"` && echo TAG: v$REPO_VERSION && git commit -a -m v$REPO_VERSION && git push && git tag v$REPO_VERSION && git push --tags;",
    "repo-publish": "npm run clean && npm i && npm run repo-publish-quick",
    "repo-publish-quick": "npm run build && npm run test && npm run repo-tag && npm publish --access public --registry https://registry.npmjs.org ",
    "repo-stage": "npm run clean && npm i && npm run repo-stage-quick",
    "repo-stage-quick": "npm run build && npm run test && npm stage publish",
    "repo-stage-list": "npm stage list",
    "repo-stage-approve": "npm stage approve"
  },
  "license": "MIT",
  "files": [
    "dist",
    "bin",
    "LICENSE"
  ],
  "bin": {
    "tabnas-railroad": "bin/tabnas-railroad"
  },
  "exports": {
    ".": "./dist/railroad.js"
  },
  "peerDependencies": {
    "@tabnas/parser": ">=0"
  },
  "devDependencies": {
    "@tabnas/debug": "*",
    "@tabnas/json": "*",
    "@tabnas/parser": "*",
    "@types/node": "25.6.0",
    "typescript": "6.0.3"
  },
  "engines": {
    "node": ">=24"
  }
}
